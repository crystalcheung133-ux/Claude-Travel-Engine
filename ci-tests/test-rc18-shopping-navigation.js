const fs=require('fs'),assert=require('assert');
const index=fs.readFileSync('index.html','utf8'), day=fs.readFileSync('day.html','utf8');
const guide=fs.readFileSync('guide-runtime.js','utf8'), data=fs.readFileSync('data.js','utf8');
const directory=fs.readFileSync('shopping-directory-data.js','utf8');
assert(!index.includes('home-shopping-button'),'Home must not add a Shopping List button');
assert(!index.includes("openGuideCategory('PRACTICAL')"),'FX/cash must not be a Guide menu category');
assert(!/"PRACTICAL"\s*:\s*\[\s*\{\s*"key"\s*:\s*"cash-backup"/m.test(data),'cash-backup must not be Guide inventory');
assert(day.includes('openShoppingDirectoryView(${Number(day)||1})'),'Timeline Shopping List must open the current-day directory');
assert(guide.includes("if(semantic==='SHOP')"),'SHOP Guide category must remain');
assert(guide.includes('Shopping Directory'),'SHOP chooser must contain the directory shortcut');
assert(directory.includes('Dear José')&&directory.includes('KIDO Studiowear')&&directory.includes('11 Garmentory'),'V4 Shopping Directory editorial set must be preserved');
console.log('RC18 SHOPPING NAVIGATION: PASS');
