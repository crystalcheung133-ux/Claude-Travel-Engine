const fs=require('fs'),path=require('path');const html=fs.readdirSync('.').filter(f=>f.endsWith('.html'));let bad=[];for(const f of html){const s=fs.readFileSync(f,'utf8');for(const m of s.matchAll(/<(?:script|link)[^>]+(?:src|href)="([^"]+)"/g)){const r=m[1].split('?')[0].split('#')[0];if(!r||/^(https?:|data:|#)/.test(r))continue;if(!fs.existsSync(path.resolve(r)))bad.push(`${f}: missing ${r}`)}}if(bad.length){console.error(bad.join('\n'));process.exit(1)}console.log(`PASS: ${html.length} HTML files reference existing local CSS/JS assets`);
