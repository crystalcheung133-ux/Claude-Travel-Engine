# VN RC10 · Visual + Guide Information Contract

- Expenses and Moments use stronger V4-inspired module gradients.
- Active/selected controls use brighter green/pink states.
- Expense modal restores a money emoji and stronger typographic hierarchy.
- Restaurant Guide cards render `Signature / Must Try` from curated Master signature data instead of silently dropping untagged signatures.
- Omakase Tiger now highlights the Penthouse sunset setting at the confirmed 17:30 slot and removes the generic trading-hours reconfirm reminder.
- Trip supports configurable presentation grouping via `tripMenuGroups`; VN combines Activities + Transport while Engine taxonomy remains separate.
- Legacy navigation back to deleted `bookings.html` was removed from generic booking detail.

Local static/runtime suite: PASS. Browser Playwright must run in GitHub CI because Playwright is not installed in this local container.
